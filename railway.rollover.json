{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.rollover"
  },
  "deploy": {
    "startCommand": "node scripts/close-stale-runs.mjs",
    "restartPolicyType": "NEVER"
  }
}
